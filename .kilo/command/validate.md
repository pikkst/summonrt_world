---
description: Run validation commands
agent: code
---
# Validate

Run the full validation suite from the `summoner-world` folder:

```bash
npm run typecheck
npm run lint
npm run test
npm run build
```

Report results. If any command fails, the task is not complete.
