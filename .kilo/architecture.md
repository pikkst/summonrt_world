Never duplicate systems.

Always reuse existing code.

Prefer composition over inheritance.

Every new system must be modular.

Every system must be independently testable.

Never introduce circular dependencies.

Never bypass the Event System.

Player is always the root object.

Creatures belong to players.

World reacts to player actions.