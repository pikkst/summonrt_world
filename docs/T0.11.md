# T0.11 – Add `.gitignore` entries

## Summary

Added `.gitignore` file to the project root with the following entries:

- `dist/` – excludes build output directory from version control
- `*.log` – excludes all log files from version control
- `.env.local` – excludes local environment configuration file from version control

## Changes Made

1. **Created `.gitignore`** at project root with standard entries for build artifacts, logs, and local environment files.

## Files Modified

| File | Action |
|------|--------|
| `.gitignore` | Created |
| `SummonerWorld_Tasks.md` | Updated T0.11 status to `[x]` |

## Verification

- `.gitignore` exists in project root with 3 entries
- T0.11 marked as completed in `SummonerWorld_Tasks.md`
